import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  homescreenContainer: {flex: 1, paddingTop: 50},
  image: {
    width: '100%',
    height: '100%',
    flex: 1,
    justifyContent: 'center',
  },
});
