import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  ImageContainer: {
    width: 300,
    height: 425,
  },

  imageDetail: {
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
  },
});
